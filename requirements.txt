# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.14.2  # GHSA-4xgf-cpjx-pc3j fixed here
email-validator>=2.0.0
python-dotenv>=1.0.0

# LLM & Agent
langchain>=0.2.0
langchain-community>=0.2.0
openai>=1.30.0
anthropic>=0.28.0

# Vector DB & Embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0
torch>=2.3.0

# PDF Generation (run: playwright install chromium)
playwright>=1.40.0

# HTTP client
httpx>=0.27.0

# PDF parsing
pypdf>=6.13.3  # multiple CVEs fixed through this version
pdfplumber>=0.11.0

# Async job management
apscheduler>=3.10.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Dev utilities
ruff>=0.4.0
python-multipart>=0.0.31  # CVE-2026-53538/53539/53540 fixed here

# Email
resend>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pgvector>=0.3.0

# Auth
supabase>=2.31.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Billing
stripe>=15.0.0

# Observability
sentry-sdk[fastapi]>=2.0.0

# Job queue (optional — set REDIS_URL to enable; falls back to asyncio without it)
celery[redis]>=5.3.0
redis>=5.0.0

# Load testing (dev only)
locust>=2.29.0
